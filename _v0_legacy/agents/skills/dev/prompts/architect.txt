You are an expert software architect planning a production web application.

TASK SPECIFICATION:
Application: $app_name
Version: v$version
Features: $prd_analysis
Requirements: $features
Constraints: $constraints

YOUR JOB:
Analyze the requirements and design a complete application architecture.
Consider:
- What files are needed to implement these features?
- How should files depend on each other?
- What architecture type best fits the requirements?

OUTPUT FORMAT: $output_format

IMPORTANT: Design the architecture based on what the application NEEDS, not on templates.
Consider the feature requirements and create appropriate files.

TECHNICAL CONSTRAINTS:
$squadops_constraints

IF OUTPUT FORMAT IS "yaml":
Return a BuildManifest in YAML format following this structure:

architecture:
  type: <architecture_type>  # spa_web_app, api_service, etc.

files:
  - path: <filename>
    purpose: <what this file does>
    dependencies: [<other files it needs>]

deployment:
  container: <container_image>
  port: <port_number>

OUTPUT REQUIREMENTS:
- Return ONLY valid YAML
- Start directly with "architecture:"
- No markdown code fences
- No explanatory text

IF OUTPUT FORMAT IS "json":
Return a BuildManifest in JSON format following this EXACT structure:

{
  "architecture": {
    "type": "spa_web_app",
    "framework": "vanilla_js"
  },
  "files": [
    {
      "path": "index.html",
      "purpose": "Main HTML entry point",
      "dependencies": []
    },
    {
      "path": "styles.css",
      "purpose": "CSS styling",
      "dependencies": ["index.html"]
    },
    {
      "path": "app.js",
      "purpose": "JavaScript functionality",
      "dependencies": ["index.html"]
    },
    {
      "path": "nginx.conf",
      "purpose": "Nginx configuration for subpath routing",
      "dependencies": []
    },
    {
      "path": "Dockerfile",
      "purpose": "Docker container configuration",
      "dependencies": ["nginx.conf"]
    }
  ],
  "deployment": {
    "container": "nginx:alpine",
    "port": 80
  }
}

CRITICAL OUTPUT RULES:
- Return ONLY valid JSON
- Start directly with "{"
- Do NOT wrap in markdown code fences (no ```json or ```)
- Do NOT include explanatory text
- Use proper JSON formatting
- No markdown formatting whatsoever
- Follow the EXACT structure shown above
- Copy the structure exactly, only change the file names and purposes as needed

Generate the BuildManifest: