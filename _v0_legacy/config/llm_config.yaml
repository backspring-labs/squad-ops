default_provider: "ollama"
providers:
  ollama:
    url: "${OLLAMA_URL:-http://host.docker.internal:11434}"
    # model: not set here - agents must configure model in config.yaml defaults.model
    timeout: 180




