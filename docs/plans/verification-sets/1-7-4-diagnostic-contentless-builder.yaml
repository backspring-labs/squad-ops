# 1.7.4 DIAGNOSTIC (1.7.4 plan §3.1) — a contentless builder first attempt (F1 #1374, R1 #1372)
#
# Not a roll and not a shakeout: a non-counting cycle that runs the roll's own path with a
# deliberate fault injected at the producing agent's emission seam (#1251). The driver
# REFUSES to count a cycle whose config declares a fault, and names the fault beside every
# readout it produces; each fault is read by the seam it reached (#1310), with a two-run
# budget for a seam that sits behind a live emission or repair (plan §3.1).
#
# Shape's provenance: #1364 — 1.7.3 counted roll 1 (cyc_af7dd4ad95b0), void: a 160-token first emission with no fence
name: 1-7-4-diagnostic-contentless-builder
records_dir: var/verification_sets/1-7-4-diagnostics/contentless-builder
pre_registration: docs/plans/1-7-4-verification-set-preregistration.md
project: group_run
squad_profile: full-38
request_profile: validated-fullstack
overrides:
  fault_injection: [builder_emission_contentless]
gate_name: progress_plan_review
n_rolls: 1
gate_notes: >-
  1.7.4 verification set. Approved under the pre-registered gate policy
  (pre-registration §7) — identical text applied to every roll of this set.
  System plan validation passed; no additional judgment applied.
launch_notes: >-
  UNUSED — a diagnostic is never counted; the driver refuses a counting roll that declares
  a fault.
shakeout_notes: >-
  1.7.4 DIAGNOSTIC — the qa suite that was never emitted. NON-COUNTING by declaration before launch: this cycle injects a
  deliberate fault at the qa emission seam (#1251) to exercise L2 (#1269) — a repair that supplies the suite an emission failure lacked is retested, which no roll of the last
  three sets happened to reach. Its red is manufactured and must never be read as the squad
  failing.
loaded_checks:
  bob: >-
    from squadops.capabilities.handlers.fault_injection import FAULTS, INJECTED_TASKS;
    import inspect; from squadops.capabilities.handlers.cycle import builder;
    print('builder_emission_contentless' in FAULTS, 'builder.assemble' in INJECTED_TASKS,
    'inject_fault(' in inspect.getsource(builder))
  runtime-api: >-
    from squadops.tasks.task_types import emits_required_files;
    print(emits_required_files('builder.assemble'), emits_required_files('qa.test'))
# A diagnostic asserts no pins: it is run on the deploy the shakeout loop settled on, and
# its record states the identity it observed (#1296).
expected_config_hash_prefix: ""
expected_squad_snapshot_prefix: ""
frozen_deploy_commit: ""
frozen_image_ids: {}
