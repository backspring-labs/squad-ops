# FastAPI+React (fullstack_fastapi_react) verification set — COUNTING (6 rolls), the first authored-mode
# baseline on this stack. Same frozen deploy as the Next.js set; its own config hash (no overrides). Same deploy as the 1.6.5
# set, no overrides: validated-fullstack's own defaults ARE stack #1, so the stack is the
# request profile's fact and the driver derives it. Its resolved_config_hash differs from the
# nextjs set's and is recorded, never compared. The last FastAPI+React cycle before this arm was
# cyc_9e77f25820b5 on 2026-08-09 — nothing from 1.6.0 onward has run on this stack.
name: 1-6-5-fastapi-react
pre_registration: docs/plans/1-6-5-plan.md
project: group_run
squad_profile: full-38
request_profile: validated-fullstack
overrides: {}
gate_name: progress_plan_review
n_rolls: 6
gate_notes: >-
  1.6.5 FastAPI+React verification set. Approved under the pre-registered gate policy
  (pre-registration §6) — identical text applied to every roll of this set.
  System plan validation passed; no additional judgment applied.
launch_notes: >-
  1.6.5 FastAPI+React verification set — COUNTED roll {roll} of {n} on
  fullstack_fastapi_react. Frozen deploy per the pre-registration §4; predictions S0–S3;
  first authored-mode baseline on this stack, no bar; scoring per 1.6.3 §5; validity per §5.1.
shakeout_notes: >-
  1.6.5 FastAPI+React regression SHAKEOUT — NON-COUNTING by declaration before launch. First
  cycle on fullstack_fastapi_react since 2026-08-09 (cyc_9e77f25820b5); authored-manifest
  mode has never run on this stack. Diagnostic only, not a measurement.
loaded_checks:
  runtime-api: >-
    from squadops.capabilities.scaffold import _py_type, is_scaffoldable_stack;
    print(is_scaffoldable_stack('fullstack_fastapi_react'), _py_type('list[Participant]'))
expected_config_hash_prefix: "c4d6a2165acf"
# The identity a per-agent override moves (1.6.5 E) — resolved_config_hash does not see it.
expected_squad_snapshot_prefix: "575707c58536cf3b"
frozen_deploy_commit: "7ebdb00e"
frozen_image_ids:
  runtime-api: cdc7049d1e8e
  max: df6ca77549d4
  neo: 02d8232c572b
  nat: eab4aae01876
  bob: a7dde4c8768b
  eve: 803853cbb43c
  data: 2796786aa8ec
