name: default
description: Standard defaults for cycle creation.
defaults:
  # SIP-0086 §12a change 1: declared, never defaulted. This profile runs no output
  # validation, so no self-evaluation pass can open; 0 says so.
  max_self_eval_passes: 0
  # SIP-0108 §10i item 3: the correction protocol this profile runs. The squad
  # analyzes the failure, decides the path, then repairs.
  correction_steps: [analyze, decide, repair]
  build_strategy: fresh
  task_flow_policy:
    mode: sequential
    gates: []
  expected_artifact_types:
    - code
    - test_report
  execution_overrides: {}
  experiment_context: {}
prompts:
  build_strategy:
    label: Build Strategy
    help_text: "How to build artifacts: fresh (from scratch) or incremental (delta)."
    choices:
      - fresh
      - incremental
  notes:
    label: Cycle Notes
    help_text: Optional notes describing the experiment intent.
