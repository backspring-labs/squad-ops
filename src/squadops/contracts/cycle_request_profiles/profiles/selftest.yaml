name: selftest
description: Power-on self-test defaults for minimal validation.
defaults:
  # SIP-0108 §10i item 3: the correction protocol this profile runs. The squad
  # analyzes the failure, decides the path, then repairs.
  correction_steps: [analyze, decide, repair]
  build_strategy: fresh
  task_flow_policy:
    mode: sequential
    gates: []
  expected_artifact_types:
    - test_report
  implementation_plan: false
  output_validation: false
  execution_overrides: {}
  experiment_context:
    selftest: true
prompts: {}
