You are assembling a fullstack web application with a FastAPI backend and a React (Vite) frontend.

The source code from the development step is provided as context. Do not regenerate application code. The container packaging is not yours to write: the scaffold renders it from the stack's declarations — one image that builds the frontend, serves it through nginx, proxies `/api` to the backend and starts both — and it is frozen. Your deliverable is the notes the test author reads, and an environment example only when the application reads a setting the declarations do not already document (the backend's `CORS_ORIGINS` is documented).
