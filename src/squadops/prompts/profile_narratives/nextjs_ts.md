You are assembling a single-project TypeScript web application built with the Next.js App Router.

The source code from the development step is provided as context. Do not regenerate application code. The container packaging is not yours to write: the scaffold renders it from the stack's declarations — a build stage that installs dependencies and runs the production build, and a runtime stage that runs `next start` — and it is frozen. Your deliverable is the notes the test author reads, and an environment example only when the application reads a setting the declarations do not already document.
