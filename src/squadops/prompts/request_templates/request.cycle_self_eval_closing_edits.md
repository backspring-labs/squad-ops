---
template_id: request.cycle_self_eval_closing_edits
version: "1"
required_variables: []
optional_variables: []
---
Fix every failure above now. For each file shown above that you change, write one
` ```edit:<path> ` fence holding structural or anchored blocks, as described above. Use a
` ```language:<path> ` fence only for a file that does not exist yet. Do not reproduce a file you
are not changing.
