# SquadOps Test Harness Dependencies
#
# NOTE: This file includes agent runtime dependencies because unit tests import
# and test agent code (e.g., agents.base_agent, agents.roles.*). These
# dependencies are required for tests to import agent modules.

# Core testing framework
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-mock>=3.11.0
pytest-cov>=4.1.0
pytest-xdist>=3.3.0

# Test utilities
faker>=19.0.0
freezegun>=1.2.0
responses>=0.23.0
httpx>=0.24.0
requests>=2.31.0  # HTTP client for integration tests

# Database testing
pytest-postgresql>=5.0.0
pytest-redis>=3.0.0
psycopg-binary>=3.0.0  # Binary PostgreSQL adapter (includes libpq, no system dependencies)

# Agent runtime dependencies (required for unit tests to import agent code)
aiofiles>=23.0.0  # Async file I/O (used by base_agent)
aio-pika>=9.3.0  # RabbitMQ async client
asyncpg>=0.29.0  # PostgreSQL async driver
sqlalchemy>=2.0  # Sync Engine/sessionmaker used by adapters/persistence/postgres (issue #206)
python-jose[cryptography]>=3.3  # JWT validation used by adapters/auth/keycloak (issue #191)
# Upper bound pins the test env to the FastAPI the project actually runs (0.135.x).
# Starlette >=0.136 newly rejects including a router instance into more than one
# app, which the console unit tests do via module-level singleton routers +
# sys.modules re-imports. The production app is unaffected (single import/include);
# this is a test-harness incompatibility. Lift the cap once #198 reworks those tests.
fastapi>=0.104.0,<0.136  # API framework (see #198 before raising the cap)
python-multipart>=0.0.6  # Required by FastAPI for form/file uploads
uvicorn>=0.24.0  # ASGI server
aiohttp>=3.9.0  # HTTP client
redis>=5.0.0  # Redis client
pydantic>=2.5.0  # Data validation
python-dotenv>=1.0.0  # Environment variable management
jsonschema>=4.20.0  # JSON schema validation
pyyaml>=6.0  # YAML parsing

# OpenTelemetry packages (for telemetry testing)
opentelemetry-api>=1.22.0
opentelemetry-sdk>=1.22.0
# NB: `opentelemetry-instrumentation-aiohttp` does not exist on PyPI (the real
# packages are `-aiohttp-client` / `-aiohttp-server`). It was masked locally by
# a `platform_machine != "aarch64"` marker — our dev box (DGX Spark) is aarch64,
# so it was skipped there but broke install on x86_64 CI. It is unused by the
# code and the suite passes without it, so it is omitted. Add `-aiohttp-client`
# if aiohttp client instrumentation is ever actually needed.
opentelemetry-instrumentation-asyncpg>=0.43b0
opentelemetry-instrumentation-aio-pika>=0.43b0
opentelemetry-exporter-otlp-proto-grpc>=1.22.0
opentelemetry-exporter-prometheus>=0.45b0  # Note: Still in beta, use 0.x version
opentelemetry-semantic-conventions>=0.43b0
prometheus-client>=0.19.0

# Additional agent dependencies
# Cap to the 0.3.x line the project runs: chat_executor.py imports
# `new_agent_text_message` from `a2a.utils`, which a2a-sdk >=0.4 removed/renamed.
# (Same unpinned-dep drift class as the fastapi cap — see #198 re: pinning deps.)
a2a-sdk[http-server]>=0.3.0,<0.4  # A2A protocol (used by chat executor)
beautifulsoup4>=4.12.0  # HTML parsing (used by qa skills)
psutil>=5.9.0  # System utilities (used by lead agent)

# Memory system dependencies (for integration tests)
lancedb>=0.8.2
pyarrow>=14.0.0
pandas>=2.0.0
sentence-transformers>=2.7.0

# Integration testing with real services
testcontainers>=3.7.0
testcontainers[postgres]>=3.7.0
testcontainers[rabbitmq]>=3.7.0
testcontainers[redis]>=3.7.0

# Mocking and fixtures
factory-boy>=3.3.0
model-bakery>=1.12.0

# Performance testing
pytest-benchmark>=4.0.0
memory-profiler>=0.61.0

# Code quality
ruff>=0.1.0
flake8>=6.0.0
black>=23.0.0
isort>=5.12.0
mypy>=1.5.0

# Documentation
sphinx>=7.0.0
sphinx-rtd-theme>=1.3.0
