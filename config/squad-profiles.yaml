profiles:
  - profile_id: full-squad
    name: "Full Squad"
    description: "All 5 agents with default models"
    version: 1
    agents:
      - agent_id: max
        role: lead
        model: gpt-4
        enabled: true
      - agent_id: neo
        role: dev
        model: gpt-4
        enabled: true
      - agent_id: nat
        role: strategy
        model: gpt-4
        enabled: true
      - agent_id: eve
        role: qa
        model: gpt-4
        enabled: true
      - agent_id: data
        role: analytics
        model: gpt-4
        enabled: true

active_profile: full-squad
