## What

<!-- What changed and why, readable standalone. -->

## Closes

<!-- One line per issue this PR FULLY resolves — GitHub only auto-closes on these keywords:
       Closes #NNN
     Partially addressed (say what remains):
       Refs #NNN — remaining: …
     No issue behind this PR (say why):
       No issue: …
     The pr-closure check fails without one of these. Bold headings and bare #NNN do not close. -->

## Evidence

<!-- Tests run, replay, cycle ids, records — what shows it works.
     Seam changes (a new output key, parameter, or row another component consumes) — name
     the live caller the wiring test enters at (TEST_QUALITY_STANDARD anti-pattern 6a):
       Entry point exercised: <executor method / protocol / registry> — <test name>
     A new or newly bound typed check — say where the task's criteria are evaluated and on
     which tree, including the outcome when the file is absent (CLAUDE.md "Typed checks"):
       Evaluated at: emission <tree> · agent repair <tree> · verifier <tree> · gate · retest
     Removing something — say what evidence it produced and who consumed it (#1253 → #1255):
       Removed: <thing> — produced <evidence> for <consumer>; replaced by <what>
     Extraction / move PRs (#1149): the rationale was harvested BEFORE the move — cite it:
       Rationale: SIP-NNNN harvest entries N–M (or "none load-bearing, because …") -->
