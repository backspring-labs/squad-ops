server {
  listen 80;
  server_name _;

  location /hello-squad/ {
    alias /usr/share/nginx/html/;
    try_files $uri $uri/ /index.html;
  }
}
server {
  listen 80 default_server;
  server_name localhost;

  return 301 http://$host/hello-squad/;
}