server {
  listen 80;
  server_name localhost;
  location /hello-squad/ {
    alias /usr/share/nginx/html/;
    try_files $uri $uri/ /index.html;
  }
}

location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg)(\?.*)?$ {
  expires max;
  access_log off;
}

rewrite ^/$ /hello-squad/ permanent;