# Release Manifest: run-001

**Run ID:** run-001  
**PID:** PID-001  
**Release Date:** 2025-10-05  
**Version:** v0.1-warmboot-001  

## Infrastructure Versions

```yaml
infrastructure:
  docker_compose: "2.20.0"
  rabbitmq: "3.12-management"
  postgresql: "15-alpine"
  redis: "7-alpine"
  prefect: "2.14.0"
```

## Agent Versions

```yaml
agents:
  max:
    role: "lead"
    model: "llama3.1:8b"
    llm_mode: "real"
    status: "online"
  neo:
    role: "dev" 
    model: "qwen2.5:7b"
    llm_mode: "real"
    status: "online"
```

## Application Versions

```yaml
applications:
  hello_squad:
    version: "1.0.0"
    technology: "Vue.js 3 + Express.js"
    port: 3000
    status: "deployed"
    endpoints:
      - "/api/hello"
      - "/hello"
```

## Deployment Commands

```bash
# Deploy run-001
git checkout v0.1-warmboot-001
docker compose up -d

# Verify deployment
curl http://localhost:3000/api/status
```

## Rollback Commands

```bash
# Rollback to previous version
git checkout v0.0-main
docker compose down
docker compose up -d
```

## Health Checks

- **Application**: http://localhost:3000/api/status
- **Health Dashboard**: http://localhost:8080
- **RabbitMQ Management**: http://localhost:15672
- **PostgreSQL**: localhost:5432

## Dependencies

- Docker Desktop
- Ollama with llama3.1:8b and qwen2.5:7b models
- Ports 3000, 8080, 5432, 6379, 15672 available
